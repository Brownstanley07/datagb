class AppConstants {
  AppConstants._();

  // Matches LMS Skill Track's logger behavior: logs remain available in
  // release builds while developer mode is enabled.
  static const bool isDeveloperMode = false;
}
